#include "main.h"
#include <stdio.h>

/**
 * mul - multiplies two integers
 * @x: The first number to multiply
 * @y: The second number to multiply
 *
 * Return: The result of multiplying x and y integers
 */

int mul(int x, int y)
{
	return (x * y);
}
